{
  "name": "seat-manager-cloudflare-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "node --test test/*.test.js",
    "check": "node scripts/check.mjs && node --test test/*.test.js"
  },
  "devDependencies": {
    "miniflare": "4.20260625.0",
    "wrangler": "^4.0.0"
  }
}
