# 版本治理与自然语言发布规范

本文是 Zhang edition 与 Commercial edition 的功能分类、版本差异和发布意图唯一来源。新功能、版本专属需求或线上发布开始前必须先按本文分类；视觉实现仍遵守 `DESIGN_SYSTEM.md`。

## 稳定模型

- 只有一套 React 源码、业务状态、数据格式和 Worker 公共契约。
- Zhang 是个人使用与先行验证通道，`main` 前端变更通过检查后自动发布。
- Commercial 是稳定通道，只能发布已在 Zhang 验证并由用户明确批准的完整 commit SHA。
- 版本差异只能控制入口、文案和能力开放，不得改变学生、座位、成绩、备份、同步等公共数据语义。
- 不复制整页或状态控制器，不维护长期 `zhang` / `commercial` 业务分支，不为同一功能创建两套存储格式。

## 自然语言指令契约

| 用户表达 | 固定解释 | 实施与发布行为 |
| --- | --- | --- |
| “先在小张版测试”或未说明版本 | Zhang 先行功能 | 进入公共源码并自动发布 Zhang；Commercial 保持当前稳定 SHA，不添加永久版本开关 |
| “只给小张版” | Zhang 永久专属 | 入口通过 `editionCapabilities` 集中控制，底层复用公共业务能力 |
| “上线商用版” | 晋升已验证版本 | 核对工作区和 Zhang 验收，取得完整 SHA，运行全套检查后触发 `promote-commercial.yml` |
| “商用版不要上线” | 不进入 Commercial | 明确转为 Zhang 专属或删除；不得长期保留含糊的“测试中”状态 |

自然语言出现冲突时，以用户最后一次明确的版本范围为准。涉及登录、存储、同步、AI写入边界或公共接口的高风险变更，即使说“先测试”，也必须保持对当前 Commercial 的向后兼容。

## 功能矩阵

每个非纯修复功能增加或更新一行。`首次 Zhang 版本` 使用 commit 短 SHA；尚未提交时写 `pending`。

| 功能 | 分类 | 首次 Zhang 版本 | Commercial 状态 | 最终决定 |
| --- | --- | --- | --- | --- |
| 班级职务、课代表与组舍长 | 公共功能 | ce1cae7 | 批准晋升 | 两版共同提供；支持自定义职位、多人兼任与成员范围校验，按班级学期保存 |
| 自定义跟进类型与登记再次点击恢复 | 公共功能 | ce1cae7 | 批准晋升 | 两版共同提供；出勤与作业 6 秒内恢复单次登记，任务类型按班级学期保存 |
| 统一授权码登录 | 公共基础能力 | pending | 待晋升 | 两版统一认证，保留独立品牌和部署 |
| 产品登录记住 90 天 | 公共功能 | pending | 待晋升 | 两版共用持久登录与 Worker 90 天上限；旧凭证保留原到期时间，未勾选和本地预览仍为临时会话 |
| Zhang 先行发布通道 | 发布治理 | pending | 不适用 | Zhang 自动发布，Commercial 人工晋升 |
| 学生档案显式编辑模式 | 公共功能 | f7e2f5f | 批准晋升 | 默认只读查看，显式进入编辑并统一使用设计系统分段控件 |
| 学生详情标签滑动切换 | 公共功能 | 490bc82 | 批准晋升 | 两版统一使用滑动指示条、方向性内容切换和内容驱动弹窗高度 |
| 授权运营后台 | 内部诊断 | pending | 不适用 | 独立管理员页面提供渠道、到期、AI、设备看板和授权检索，不进入产品前端 |
| 宿舍周期统计与补录日期 | 公共功能 | d8c0ef7 | 批准晋升 | 统一按事件日期查看周/月/重复自定义周期净分，移除基础分、结转和手动结算，兼容旧归档事件 |
| 可撤销操作反馈与班级跟进 | 公共功能 | 621f997 | 批准晋升 | 显式创建、保存和状态更新统一使用可撤销 ActionToast；跟进任务可作为不绑定学生的班级事项 |
| 自定义座位布局与分组拓扑 | 公共功能 | 7b13ec7 | 批准晋升 | 保留八列四组默认布局；提供批量框选、孤立槽位、框选成组、组名编辑、讲台设置和吸附动画，编辑与解除兼容框选模式；行列边缘使用局部悬停淡绿加号/淡红减号，含座位删除须确认，插入不闪烁；新增和边缘扩展共用碰撞提示/确认，只转移相交座位，旧组收缩，断开区域自动拆组；组轮廓不重叠并避让讲台，排座读取相邻关系与分组关系 |
| 今日班务、作业、周报与题目分析工作台 | 公共功能 | bb7bc1d | 批准晋升 | 今日只编排现有领域；课表、快捷记录、出勤和作业本地优先；出勤与作业使用固定顺序的快速登记和可撤销反馈；周报与题目分析按需 AI 增强，不改变计费方式 |
| 跨领域闭环、动作流水与业务生命周期 | 公共功能 | pending | 批准晋升 | 统一来源定位、任务结果/继续跟进、学生归档、作业生命周期、沟通稿分享与真实本地动作流水；Commercial 仅在本轮完整验收并取得 SHA 后显式晋升 |
| 全局 AI Companion 浮窗 | 公共功能 | pending | 批准晋升 | 用右下常驻入口替代侧栏独立 AI 页面；对话跨业务页保留、小屏全宽适配，继续复用产品授权、工作区缓存与教师确认写入边界 |
| 评语选区 AI 局部润色 | 公共功能 | pending | 批准晋升 | 评语工作台选中文字后提供优化表达、更具体和缩短；AI 只返回局部替换建议，教师确认应用后仍需显式保存 |
| 高频班务与一致性优化 | 公共功能 | pending | 批准晋升 | 修复数据保留与跨页状态缺陷，降低首屏和持久化开销，并增强出勤、班费、今日队列、学生检索、成绩阈值、撤销和键盘无障碍；不改变公共数据与教师确认边界 |
| 成绩排名补全与赋分兼容 | 公共功能 | 7b13ec7 | 批准晋升 | 成绩导入由老师确认是否补齐缺失班排，保留原始分、赋分、既有班排和校排；每科展示排名，进退步统一按班排判断，旧成绩与备份继续兼容 |
| 多人事项与逐人跟进一致性 | 公共功能 | 7b13ec7 | 批准晋升 | 共同事项一条待办关联多人，逐人作业/成绩/出勤/催缴继续独立；统一编辑、来源复用、历史、学生删除和持久化，历史拆分记录不自动合并 |
| 座位恢复与评语确认保存可靠性 | 公共功能 | ce1cae7 | 批准晋升 | 完整座位撤销、稳定学生 ID 快照、名单重排守锁；评语草稿显式保存、批量确认、异步结果隔离和同工作区保存桥接。验收通过后先发布 Zhang，再以同一提交晋升 Commercial |
| 班主任工作流补全与座位轮换历史 | 公共功能 | pending | 待晋升 | 沟通稿历史重开、任务计划日期、跨日请假返校、班费逐人收费闭环；座位轮换参考稳定 ID 历史并记录可撤销快照，同源作业跟进按一件事项展示、逐人记录仍独立。先完成 Zhang 验证，再按完整 SHA 晋升 Commercial |

| 手机工作区适配与点选调座 | 公共功能 | 386dd06 | 批准晋升 | 手机导航、分栏切换、触屏调座与弹窗适配，保留桌面动效和数据契约；用户已授权合并推送 main 并发布 Commercial，先完成 Zhang 检查和部署，再以同一完整 SHA 晋升 |
| BoardUI 统一组件与工作区动效 | 公共功能 | ce1cae7 | 批准晋升 | 复用统一令牌与基础控件，保持座位编辑连续过渡、可中断的页面/弹层切换和减少动态效果；评语工作台使用主框架，切换学生只移动内容 |
| 原位排座与编辑动效 | Zhang 先行 | pending | 待晋升 | 排座规则和方案预览留在座位工作区，座位与等待区连续过渡且仍可操作；宿舍与学生资料编辑、评语自定义输入沿用同一元素平滑切换，浮窗遮罩覆盖侧栏。2026-09-23 用户已查看本地预览并授权合并推送 main，Commercial 需单独授权晋升 |

允许的分类只有：`公共功能`、`Zhang 先行`、`Zhang 专属`、`Commercial 专属`、`内部诊断`。同一时间尽量只保留少量 `Zhang 先行`；验证结束必须晋升、转专属或删除。

2026-08-31：用户已完成人工验收并明确批准将座位编辑、成绩排名/删除修复及多人事项一并发布到两版。业务提交为 `7b13ec7be37560f2423227528a745ddab93feb68`；发布仍须先完成 Zhang 部署，再以包含本治理记录的同一完整 SHA 晋升 Commercial，不以批准记录代替部署成功。

## 能力开关规则

- 永久差异只在 `frontend-react/src/app/editionCapabilities.ts` 声明。
- 页面读取有业务含义的能力名，不直接散落新的 `APP_EDITION` / `IS_COMMERCIAL` 判断。
- 先行功能依靠发布 SHA 差异，不创建永久开关。
- 专属功能可以增加入口、工具、报表和工作流；若要改变公共数据模型或核心业务含义，必须先重新评审为公共能力。
- Commercial 构建不得暴露内部诊断入口；两版必须共同通过持久数据兼容测试。

## Commercial 晋升与回滚

用户说“上线商用版”后，Codex按以下顺序执行，不得跳步：

1. 检查 dirty worktree、提交范围和目标 SHA，保留无关用户文件。
2. 确认目标是 `main` 上完整的 40 位 SHA，且该 SHA 的 Zhang Pages 部署成功并已获用户验收。
3. 运行设计检查、lint、strict typecheck、覆盖率、双版本构建、体积/生产包检查、双版本浏览器测试和相关 Worker 检查。
4. 触发 `Promote verified Zhang release to Commercial`，参数只允许该 SHA。
5. 验证 Commercial 线上登录、工作区、本地数据读取、PWA更新提示和本次功能；失败立即停止并报告。

回滚不是修改代码：重新运行同一 workflow，传入上一稳定的 40 位 SHA。每次交接必须记录当前晋升 SHA；无法确认 Zhang 验收或测试失败时不得发布。

## 授权与兼容边界

- `APP_EDITION` 表示品牌与发布通道；`AUTH_MODE` 表示认证方式。当前两版均为 `license`。
- 授权记录用 `allowedEditions` 控制 `zhang`、`commercial` 或两版通用。缺少字段的旧记录默认仅 Commercial。
- Zhang 首次切换前必须先在授权后台创建 Zhang 或两版通用授权。
- 旧本地密码 hash 不删除，但不再提供本地密码登录或修改入口。
- 授权切换不得迁移、清空或重写 `seat-manager-workspaces-v1`；首次线上验收前先导出本机备份。
- 两版 AI 均复用产品授权 token，由授权记录的 `aiEnabled`、`aiExpiresAt` 和 `aiDailyLimit` 控制；旧独立 AI 使用码仅作后端兼容。AI模型、DeepSeek Key、额度算法和确认写入边界不因版本治理改变。

2026-09-13：用户查看本地预览后明确要求提交并推送本轮 17 项缺陷修复，同时包含此前等待区顺序、空座落地和拖动回归改动，并同步发布 Commercial。授权范围为本轮公共源码完整提交；先确认该 SHA 的 Zhang 部署成功，再以同一完整 SHA 晋升 Commercial。共享 Worker 的 AccountCoordinator 首次迁移先备份授权 KV，线上验收只读，不修改真实授权或教师数据。

2026-09-22：用户已验收协作任务的 BoardUI 动效，并明确授权本次功能修复验收后合并 `codex/boardui-preview` 到 `main`，先发布 Zhang，再将同一完整 SHA 发布 Commercial。上述三项先行功能转为公共功能；发布记录以对应 GitHub Actions 成功结果为准，不能把本地验收或授权记为部署成功。保留主目录无关未提交改动；本轮未变更 Worker 路由、KV 或授权契约。

本轮业务基线为 `ce1cae798daff35d2f5ee4438549d1cfd4cd036f`；实际晋升采用包含本治理记录的完整 main SHA。完整验收结果见 `BOARDUI_WORKBENCH_AUDIT.md`，部署状态以对应 SHA 的 Zhang / Commercial 发布任务为准。

2026-09-22（手机适配）：用户明确要求将 `codex/mobile-usability` 合并推送 main，并追加授权 Commercial 同步发布。本轮业务提交为 `386dd06`；先确认包含本记录的 main 完整 SHA 的 Zhang 检查和部署成功，再以该 SHA 晋升 Commercial。主目录其他任务的未提交改动不进入本次发布。验证记录见 `MOBILE_USABILITY.md`，实际部署状态以两条对应 SHA 的工作流为准。
